# MVP Completion — `/speckit.clarify` and `/speckit.analyse`

**Feature:** `specs/004-mvp-completion/`
**Date:** 2026-08-25
**Baseline:** `origin/main` @ `a4bb356` (refreshed 2026-08-30; first drafted against `5b4b77c`). Trunk advanced to `ebe77af` on 2026-08-31 when PR #172 merged; see `spec.md` §3.

---

## Clarifications resolved with the integration lead

Four questions materially changed the artifacts. All four were put to the integration lead and answered; none was resolved by assumption.

| ID | Question | Resolution |
|---|---|---|
| **Q-1** | What counts as "complete" for `T0513`? | **Zero open backlog.** All open non-epic *backlog* issues close before release — 16 when this was decided, 14 as of 2026-08-31 (#171 fixed, #134 closed) — not all 26 non-epic issues (28 when decided); the other 12 are the 10 package issues and 2 rollup trackers, which close by delivery — not only those a §19.6 gate or a T-task requires. This is the widest of the three scopes offered; #135, #137, #138 and #143 become release blockers despite no gate demanding them. Recorded as a risk in `spec.md` §9 so the choice stays visible |
| **Q-2** | How should the unowned live-data cutover be represented? | **Register a new `RELEASE-LIVE-CUTOVER` package** with a `credentials:` override, absorbing #132, #137, #56/`T0112` and #81, and make `M5-AUDIT-RELEASE` depend on it. Rejected: widening #68's scope in place, which leaves provisioning invisible until wave 6 |
| **Q-3** | Is `M3-CONFIDENCE-GATE`'s dependency on `M3-REVIEW` real, given zero path overlap? | **Real; the edge stays.** #62 enforces the review thresholds #61 implements, and its no-auto-approval tests require a review workflow to exist. Critical path remains 6 deep. Rejected: breaking the edge for a 6→5 shortening, which would have let #62 run parallel to #61 |
| **Q-4** | What should this feature directory contain, given the constitution names `001/` as sole canonical? | **`spec.md` + `plan.md` + `clarify-analyse.md`; no `tasks.md`.** The outstanding work already carries canonical IDs in `001/tasks.md`, and a fourth ledger of the same rows would reproduce the drift documented in `spec.md` §4.2. `plan.md` lands with an explicit subordination header; the constitutional tension is recorded as **C-1** below for the lead to rule on |

---

## Analysis findings and disposition

Severity reflects consequence if unaddressed, not effort to fix.

| ID | Severity | Finding | Disposition |
|---|---|---|---|
| **A-1** | HIGH | The replace-OpenAI directive recorded in #132 substitutes `claude-opus-4-8` for ADR-0002's accepted "OpenAI for generation", with no superseding ADR and no benchmark evidence. Constitution principle V names additional AI providers explicitly and requires both | **Unresolved — needs a superseding ADR before `RELEASE-LIVE-CUTOVER` selects any provider.** Either choice violates something today: following the directive breaches ADR-0002, following ADR-0002 contradicts a standing directive. `plan.md` records this as a failed Constitution Check with a Complexity Tracking entry |
| **A-2** | HIGH | `M5-AUDIT-RELEASE` (#68) inherits `defaults.credentials: mock-only` yet owns `T0214b` (dual-adjudicated ≥300-question benchmark over ≥20 real issuers) and `T0513`. The dependency graph terminates at a package whose inherited credential setting does not provide what its acceptance criteria require (`credentials:` is a provisioning descriptor, not a prohibition) | **Resolved in design by Q-2** — register `RELEASE-LIVE-CUTOVER` and add the edge. Not resolved in fact until the lead files the issue and provisions credentials. Root cause is schema-level: `depends_on` cannot express a precondition outside the repository |
| **A-3** | HIGH | The canonical ledger `001/tasks.md` reports 21 of 67 in-scope tasks complete; measured against merged packages the figure is 38. Seventeen delivered tasks still read `[ ]` | **Reconcile in a follow-up PR**, not this one — `001/tasks.md` is a shared path and a separate authorization. Wave 0 is a set of lead actions spanning several PRs, of which only the #171 fix gates wave 1; the reconciliation does not block dispatch. `spec.md` §4.2 lists the exact 17 and flags that `T0215` should be confirmed against `packages/retrieval/ACCEPTANCE.md` rather than inferred from #58's merge |
| **A-4** | HIGH | Constitution principle IV (secrets via approved flows) cannot be asserted: all five credential groups in `CREDENTIALS.md` read "Not requested", so the flow has never been exercised | **Assigned to `RELEASE-LIVE-CUTOVER`.** Recorded as a failed Constitution Check in `plan.md` with a Complexity Tracking justification |
| **A-5** | MEDIUM | M2's exit criterion M2-024 is not met — #132 is open and PR #122 merged mock-first plumbing only — yet M2 is described as complete in the handoff documents | **Folded into `RELEASE-LIVE-CUTOVER`.** `spec.md` §3 states M2 is *code*-complete rather than complete, and §4.3 lists #132 as release-blocking |
| **A-6** | MEDIUM | `CREDENTIALS.md` offers "a paid tier ... **or** an ADR revisiting the market-data adapter choice" as a live either/or. ADR-0002 (Accepted) already ruled the paid tier required, and its change rule admits a superseding ADR only on benchmark evidence that the current default fails a requirement. The handoff document therefore advertises an escape hatch the accepted ADR has closed | **Corrected in `spec.md` §5.2** — reframed from "unresolved product decision" to budget authorization against an accepted ADR, with the either/or contradiction named. `CREDENTIALS.md` itself should be amended to drop the second branch or cite the evidence ADR-0002 requires; that is a `docs/handoff/**` edit outside this PR's scope |
| **A-7** | MEDIUM | #134 tracked work that already merged: PR #131 @ `bc6a2a3` landed Playwright E2E in CI but declared no closing issue, so #134 never auto-closed and read as pending release-blocking work | **RESOLVED 2026-08-29 — #134 closed.** Under the Q-1 zero-backlog scope this stale issue would otherwise have blocked release on work already done |
| **A-8** | MEDIUM | Trunk CI was green at `5b4b77c` while carrying the #171 over-suppression regression, because no test covered the defect | **RESOLVED 2026-08-29 — PR #174 @ `a4bb356`**, red-green verified, issue closed. The general point survives its instance and a live example remains: the full 1009-test Python suite still passes with the `_check_segment_sums` suppression mechanism deleted outright, so the mechanism's *lower* bound is untested. Worth its own issue |
| **A-9** | MEDIUM | Path-contention analysis uses exact-string comparison of `allowed_paths` globs and cannot detect subtree containment — `evals/reporting/**` is a subtree of `evals/**` but compares as distinct | **Documented as a method caveat** in `spec.md` §6.1. `workstreams.yaml` already flags this exact case for `EVALS-REPORT-RENDER`. Containment must be checked by hand before dispatching a narrowed path against a broad one |
| **A-10** | LOW | Checking a ledger box asserts the parent's completion standard — "code, tests, telemetry, documentation, and acceptance evidence are present" — which merge alone does not establish | **Caveat recorded** in `spec.md` §4.2 against the A-3 reconciliation |
| **A-11** | MEDIUM | An independent requirements-coverage pass over all 53 parent-spec requirement IDs found **zero genuinely unowned requirements**. **Fourteen** are cited by no task in the canonical ledger, but thirteen of those are covered by a task's prose or by the child specifications `002/` and `003/`. Exactly one is not: `FOR-004` ("Forecast exports distinguish reported, modeled, and user-supplied values") is **neither cited by a task nor covered by task prose**. `T0510` enumerates export *formats* only. This is not mere citation hygiene: `FOR-004` is the export-side expression of constitution principle II, "Reported, derived, user-supplied, and forecast values MUST remain distinguishable" — the one constitutional guarantee with no task anchor | **Cite `FOR-004` on `T0510`** when `001/tasks.md` is reconciled under A-3, following the ledger's own convention (`T0309`→`EXT-004`, `T0408`→`MOD-002`, `004`). Ownership is unambiguous either way: `M5-AUDIT-RELEASE` (#68) holds `T0509`/`T0510` and `packages/export/**`. Nothing implements it today — `packages/export/**` does not exist on trunk |
| **A-12** | MEDIUM | `.specify/templates/plan-template.md` requires a Technical Context section, whose field list enumerates language, dependencies, storage and target platform. Constitution principle V states the locked stack "is recorded in `docs/decisions/ADR-0002-mvp-stack.md` and MUST NOT be restated elsewhere", so filling those fields verbatim would restate the stack inside the section that then self-certifies stack compliance | **Narrower than first recorded — no escalation needed.** The template's sole "in advisory capacity" note (`plan-template.md:15-19`, its only occurrence of the phrase) sits inside the Technical Context HTML comment and is scoped to that section's **field list**, not to the template's section list: the section is required, its fields are advisory. `plan.md`'s ADR pointer is therefore template-compliant as written, and no `plan-template.md` amendment is required. `plan.md:19` still describes this as a "genuine template-versus-constitution conflict" and carries the same over-broad framing; correcting it is a `specs/004-mvp-completion/plan.md` edit outside this PR |
| **A-13** | HIGH | The first draft of `plan.md` asserted that the Governance section's "a failed mandatory gate blocks merge" applies only to *implementation* work, exempting a planning artifact. The constitution draws no such distinction; the sentence is unqualified. The document declared its own gate failed, then reinterpreted the rule that would block it | **Corrected before merge.** The carve-out is removed from `plan.md` and from this file. The gate is now reported as failing three of six, with the explicit consequence that merging requires an integration-lead waiver recorded on the PR. Surfaced by independent review, not self-caught — noted because a self-granted exemption is the failure mode least likely to be caught by its own author |
| **A-14** | HIGH | `M3-CONFIDENCE-GATE` (#62) cannot complete mock-only, yet `spec.md` §6.2 gave its wave the single precondition "#61 merged" and parked `RELEASE-LIVE-CUTOVER` four waves later. #62 owns `M3-304` — *"Run live OpenAI structured-output smoke with approved secret, provider-failure suite, `make ci`, independent review, and publish immutable eval report"* (`specs/003-agentic-extraction/tasks.md`) — and its issue body repeats the live smoke and immutable report and asks for `FEL_OPENAI_API_KEY`. Like every package but #108 it inherits `defaults.credentials: mock-only`. **Root cause:** `M3-300`–`M3-303` carry `/ T0306`, `/ T0307` and `/ T0310` canonical mappings and **`M3-304` carries none**, so every figure this spec derives from the canonical ledger — the 29 outstanding tasks, §7's per-package criteria — passed straight over it. A-2 found the same mock-only defect at the terminal node and stopped there | **Resolved by resequencing.** `spec.md` §6.2 moves `RELEASE-LIVE-CUTOVER` to wave 2 and gates #62 on it; §5 widens the mock-only conclusion from the terminus to #62 onward; §7's #62 bullet now carries the live smoke, immutable report and credential request it had omitted while claiming to enumerate the issue body. **No deferral of `M3-304` is recorded — that is a ruling the lead has not made.** Not resolved in fact until the lead registers the package and provisions the secret. **Durable fix:** register #62 as `depends_on: [M3-REVIEW, RELEASE-LIVE-CUTOVER]` so the constraint lives in the graph, and give `M3-304` a canonical `T`-mapping — an untethered task is invisible to every ledger-derived count |
| **C-1** | MEDIUM | The constitution states `specs/001-financial-evidence-lab/` holds the **sole canonical** `spec.md`, `plan.md` and `tasks.md`. This feature adds the repository's second `plan.md` | **Ruled 2026-08-31 — option (b), landed as constitution 1.2.0 (PR #183 @ `b8dc8a6`, merged 2026-09-03).** The clause now names `001/` as holding the sole canonical *task ledger* plus the parent `spec.md`/`plan.md`, and permits subordinate `spec.md`/`plan.md` in milestone and completion directories, which retroactively legitimises `002/` and `003/`. The options weighed were (a) accept the subordination header in `plan.md` as sufficient, (b) amend the constitution, (c) drop `plan.md` and follow the 002/003 precedent exactly; (a) and a per-directory waiver were rejected because they leave the next directory in the same position |

---

**Result:** Q-1 through Q-4 are resolved and reflected in `spec.md` and `plan.md`. Of the analysis findings, A-6 was corrected in-place during drafting; A-3, A-5, A-7, A-8, A-9, A-10, A-11 and A-12 are recorded with named owners and follow-up routes; A-13 was a defect in this plan's own first draft, caught by independent review and corrected before merge; A-2, A-4 and A-14 are resolved in design and assigned to a package the lead must still register, A-14 additionally by resequencing that package ahead of #62; **A-1 requires an integration-lead ruling and is not resolved here; C-1 was ruled 2026-08-31 and landed as constitution 1.2.0 (PR #183 @ `b8dc8a6`).** No finding is silently deferred.

**Template conformance:** `plan.md` carries every section `.specify/templates/plan-template.md` requires, in template order, with no `Option` labels remaining and a populated `Structure Decision`. It adds one section the template does not list — `## Phasing`, between Project Structure and Complexity Tracking — because this is a sequencing plan and wave order is its substance. Disclosed here rather than left silent. The template's "in advisory capacity" note is scoped to Technical Context's field list (**A-12**) and does not license a section addition; nothing in the template forbids one either, so the addition is recorded here for the lead rather than claimed compliant.

**Coverage result:** an independent pass over all 53 parent-spec requirement IDs found **no requirement without an owner**. **Thirty-nine** are cited explicitly once range and shorthand notation is expanded (`FR-WRK-001`–`004`, `FR-RAG-005`, `007`, `008`, and similar); the remaining **fourteen** — `EXT-001`–`003`, `FOR-001`–`004`, `MOD-001`, `MOD-003` and `UX-SRC-001`–`005` — are cited by no task, and thirteen of them are covered by task prose or by the child specifications `002/` and `003/`. `UX-ATL-001`–`005` are cited, but only by the struck-through deferred tasks `T0212`/`T0213`, consistent with parent §8.1. `FOR-004` is the fourteenth and the sole weak point: uncited *and* uncovered. It is recorded as A-11.

> **Method note.** The bare `EXT-`, `FOR-` and `MOD-` IDs are distinct requirements from their `FR-`-prefixed namesakes — the parent spec defines both families. A substring grep for `EXT-001` matches `FR-EXT-001` and silently reports the bare ID as cited; five of the fourteen (`EXT-001`, `EXT-003`, `FOR-001`, `MOD-001`, `MOD-003`) are visible only under boundary-anchored matching. Anchor the pattern before re-running this count.

---

## Constitution check

Mirrors `plan.md`, which is authoritative for the gate result.

- **Evidence and temporal integrity:** preserved unchanged. This feature adds no evidence path and no retrieval surface; the 100% temporal-validity gate remains a release blocker, and the sole outstanding ingestion work (`T0112`) routes through `RELEASE-LIVE-CUTOVER` under the already-merged M1 cutoff suites.
- **Deterministic financial computation:** untouched. `M4-MODEL-CALC` (#63) carries the decimal-engine and property-test obligations verbatim from `T0403`/`T0409`; nothing here relaxes or reinterprets them.
- **Test-first quality gates:** honoured in sequencing, and now in fact. Wave 0 placed the #171 red-green regression test *before* any dispatch; it merged as PR #174 @ `a4bb356` on 2026-08-29, so the wave-1 gate has cleared. The ledger reconciliation remains outstanding but gates no dispatch.
- **Security and cost boundaries:** **cannot be asserted (A-4).** The approved secret-management flow has never been exercised — all five credential groups read "Not requested". Re-check required before wave 6.
- **Simplicity and provider isolation:** **cannot be asserted (A-1).** The replace-OpenAI directive substitutes an AI provider against ADR-0002 (Accepted) with neither a superseding ADR nor benchmark evidence, both of which principle V requires.

- **Development and Review Workflow** (feature-specific sixth check, added because the five template items are the five Core Principles and the clause this feature strains is not among them): **satisfied since constitution 1.2.0 (PR #183 @ `b8dc8a6`, 2026-09-03).** This is the repository's second `plan.md`, which 1.2.0 permits as a subordinate artifact; at authoring under 1.1.0 it could not be asserted — finding C-1.

**Gate result: FAILS, two of six** (three of six at authoring on 2026-08-31; C-1 cleared with constitution 1.2.0 on 2026-09-03). The Governance section is unqualified — *"Unjustified complexity or a failed mandatory gate blocks merge"* — and draws no implementation-versus-planning distinction. **This specification therefore could not merge on its own authority; PR #173 merged on 2026-08-31 under an explicit integration-lead waiver recorded on that pull request.** That the failures are pre-existing conditions this feature surfaces rather than introduces was an argument for granting the waiver, not a reason the gate does not apply. See **A-13**.
