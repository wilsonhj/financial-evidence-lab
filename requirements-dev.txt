# Development toolchain. Installs the runtime dependencies first so a dev
# environment and a deployed image agree on their versions.
-r requirements.txt

# Python toolchain (formatting, linting, typing, tests, security scanning).
# Minimum versions track known-clean releases; pip-audit gates the tree in CI.
ruff>=0.7.1
black>=26.3.1
mypy>=1.13.0
pytest>=9.0.3
pytest-cov>=6.0.0
pyyaml>=6.0 # apps/api/tests/test_openapi_parity.py reads packages/contracts/openapi/openapi.yaml
bandit>=1.7.10
pip-audit>=2.7.3
