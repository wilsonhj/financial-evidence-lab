# Python toolchain (formatting, linting, typing, tests, security scanning).
# Minimum versions track known-clean releases; pip-audit gates the tree in CI.
ruff>=0.7.1
black>=26.3.1
mypy>=1.13.0
pytest>=9.0.3
bandit>=1.7.10
pip-audit>=2.7.3

# Runtime dependencies exercised by API/worker unit tests.
fastapi>=0.121.0
uvicorn>=0.32.0
pydantic>=2.9.2
httpx>=0.27.2
httpx2>=2.0.0
