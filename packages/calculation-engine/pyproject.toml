[project]
name = "fel-calculation-engine"
version = "0.0.0"
description = "Financial Evidence Lab typed model graph and deterministic Decimal calculation engine (M4)"
requires-python = ">=3.11"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
include = ["fel_calculation_engine*"]
