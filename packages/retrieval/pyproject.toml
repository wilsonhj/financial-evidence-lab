[project]
name = "fel-retrieval"
version = "0.0.0"
description = "Financial Evidence Lab retrieval item builder and index helpers"
requires-python = ">=3.11"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
include = ["fel_retrieval*"]
